= stylesheet_link_tag 'home'
- unless @user
  #splash
    .row.container
      #tagline2.col-md-offset-2.col-md-5
        %h1
          Join Mumbai's
          %span.highlight crowd-sourced, real-time
          BEST bus locator.
        %h3#scrawl
          Get realtime bus updates and alerts, help your fellow commuters and win prizes!
        %div{:style => "text-align: center"}
          = link_to (image_tag 'fb-login.png'), user_omniauth_authorize_path("facebook")
          = link_to (image_tag "google.png"), user_omniauth_authorize_path(:google_oauth2)

      #android.col-md-3.col-md-offset-1
        = image_tag 'android.png'
- else
  %h3
    == welcome #{@user.name} (#{@user.email})