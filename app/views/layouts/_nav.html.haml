%nav.navbar.navbar-default{role: "navigation", style: "padding: 0px 20px"}
  / Brand and toggle get grouped for better mobile display
  .navbar-header
    %button.navbar-toggle{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", type: "button"}
      %span.sr-only Toggle navigation
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
    %a.navbar-brand{href: "/"}
      = image_tag 'header_logo.png', style: "height:50px"
  / Collect the nav links, forms, and other content for toggling
  #bs-example-navbar-collapse-1.collapse.navbar-collapse
    %ul.nav.navbar-nav.navbar-right
      -  if current_user
        %li.dropdown
          %a.dropdown-toggle{"data-toggle" => "dropdown", href: "#"}
            = profile_pic(style: "border-radius:50%;")
            = current_user.name
            %b.caret
          %ul.dropdown-menu
            %li
              = link_to "Profile", profile_path
            %li
              = link_to "Log out", destroy_user_session_path, :method => :delete
      - else
        = link_to (image_tag 'fb-login.png'), user_omniauth_authorize_path("facebook")

  / /.navbar-collapse
